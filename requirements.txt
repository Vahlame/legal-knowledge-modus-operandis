# --- Conversión del corpus (solo para reconvertir los PDFs) ---
pymupdf4llm>=0.0.17

# --- Backend neuronal: recuperación de máxima calidad (recomendado, por defecto) ---
# fastembed provee el bi-encoder (paraphrase-multilingual-MiniLM, ~0.22 GB) Y el
# reranker cross-encoder (jina-reranker-v2-base-multilingual, ~1.11 GB), que se bajan
# la primera vez. Si fastembed NO está, cae al embedder stdlib (cero dependencias).
# Overrides: LEGAL_EMBEDDER=stdlib|neural   ·   LEGAL_RERANK=0 (desactiva reranker).
fastembed>=0.4

# numpy lo arrastra fastembed; se usa para el índice denso.
